# Albiyan DC Portfolio

Personal portfolio built with Next.js App Router and plain CSS. It is ready to deploy on Vercel.

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```bash
npm run build
```

## Customize

Edit portfolio content in `app/page.tsx` and visual styling in `app/globals.css`.
